pipeline {
    agent any 

    stages {
        stage('Build') {
            steps {
                script {
                    // Build your Docker image
                    echo 'successfull'
                }
            }
        }
       
    }
}
